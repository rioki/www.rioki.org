box: wercker/node
build:
  steps:
    - wercker/npm-install
    - hgen/gulp:
        tasks: default
deploy:
  steps:
    - wercker/s3sync:
        key_id: $KEY
        key_secret: $SECRET
        bucket_url: $URL
        source_dir: build
