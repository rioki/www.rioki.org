box: wercker/nodejs@1.0.2
build:
  steps:
    - npm-install
    - hgen/gulp:
      tasks: default
deploy:
  steps:
    - s3sync:
        key_id: $KEY
        key_secret: $SECRET
        bucket_url: $URL
        source_dir: build